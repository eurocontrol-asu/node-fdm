[project]
name = "axm-fdm-workspace"
version = "0.0.0"
description = "Physics-guided Neural ODE framework for aircraft flight dynamics"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "EUPL-1.2" }
authors = [{ name = "Gabriel Jarry", email = "gabriel.jarry@eurocontrol.int" }]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Typing :: Typed",
]
dependencies = [
    "node-fdm-data",
    "node-fdm",
    "node-fdm-bada",
    "node-fdm-pipeline",
]

[project.urls]
Homepage = "https://github.com/eurocontrol-asu/axm-fdm-workspace"
Documentation = "https://eurocontrol-asu.github.io/axm-fdm-workspace/"
Repository = "https://github.com/eurocontrol-asu/axm-fdm-workspace.git"
Issues = "https://github.com/eurocontrol-asu/axm-fdm-workspace/issues"

[tool.uv.workspace]
members = ["packages/*"]

[tool.uv.sources]
node-fdm-data = { workspace = true }
node-fdm = { workspace = true }
node-fdm-bada = { workspace = true }
node-fdm-pipeline = { workspace = true }

[dependency-groups]
dev = [
    "pre-commit>=4.0",
]

[tool.ruff]
target-version = "py312"
line-length = 99
src = ["packages/*/src", "packages/*/tests"]
exclude = ["*/_version.py"]

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "B", "C4", "S", "T20", "RUF"]
ignore = ["S101"]

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["S101", "S105", "S106", "S108", "S603", "S607"]
"scripts/**" = ["T201", "N999", "N806", "S110"]

[tool.ruff.lint.isort]
known-first-party = ["node_fdm_data", "node_fdm", "node_fdm_bada", "node_fdm_pipeline"]

[tool.pytest.ini_options]
testpaths = ["packages/node-fdm-data/tests", "packages/node-fdm/tests", "packages/node-fdm-bada/tests", "packages/node-fdm-pipeline/tests", "tests"]
addopts = "-q --import-mode=importlib"

[tool.coverage.run]
source_pkgs = ["node_fdm_data", "node_fdm", "node_fdm_bada", "node_fdm_pipeline"]
branch = true

[tool.coverage.report]
show_missing = true
fail_under = 80
exclude_lines = ["pragma: no cover", "if TYPE_CHECKING:", "raise NotImplementedError"]

[tool.mypy]
python_version = "3.12"
strict = true
warn_unused_configs = true
warn_unused_ignores = true
show_error_codes = true

[[tool.mypy.overrides]]
module = ["tests.*", "conftest"]
disallow_untyped_defs = false
disallow_untyped_decorators = false

[[tool.mypy.overrides]]
module = ["torch.*", "torchdiffeq.*", "scipy.*", "polars.*", "yaml.*", "cyclopts.*", "structlog.*", "traffic.*", "pandas.*"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = ["node_fdm_pipeline.commands.data"]
disallow_untyped_calls = false

[[tool.mypy.overrides]]
module = ["node_fdm_pipeline.cli"]
disallow_untyped_decorators = false

[[tool.mypy.overrides]]
module = ["scripts.*"]
ignore_missing_imports = true
disallow_untyped_defs = false
disallow_untyped_decorators = false
